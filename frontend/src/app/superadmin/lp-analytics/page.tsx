import LpAnalyticsClient from './LpAnalyticsClient';

export const dynamic = 'force-dynamic';
export const revalidate = 0;

export default function LpAnalyticsPage() {
  return <LpAnalyticsClient />;
}
